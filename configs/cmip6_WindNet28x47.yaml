defaults:
  - raw: cmip6_elevation_paths              # paths to raw data to prepare dataset
  - process: cmip6_elevation_dataset        # dataset processing configuration
  - train: train_WindNet28x47  # train configuration and data load configuration

experiment_name: cmip6_linear_model
project_name: Wind-speed-regression
target_type: wind_ms #*
cmip_type: CMIP6 #*

model_name: WindNet28x47  # {name}{time_window}x{half_side_size}
time_window: 28               # must match model, odd only
half_side_size: 47            # must match model, odd only

#* have no effect, just info ß