defaults:
  - raw: cmip5_paths                    # paths to raw data to prepare dataset
  - process: cmip5_dataset              # dataset processing configuration
  - train: train_WindNet41x41_dist  # train configuration and data load configuration
  - eval: test_WindNet41x41             # test and eval configuration 

experiment_name: cmip5_reg
project_name: Wind-speed-regression
target_type: wind_ms 
cmip_type: CMIP6 

model_name: WindNet41x41      # {name}{time_window}x{half_side_size}
time_window: 41               # must match model, odd only
half_side_size: 41            # must match model, odd only

