defaults:
  - raw: cmip6_elevation_paths    # paths to raw data to prepare dataset
  - process: cmip6_dataset        # dataset processing configuration
  - train: train_cmip6     # train configuration and data load configuration
  - eval: test_GhostWindNet

experiment_name: cmip6_GhostWindNet27x61_3vars_quantile_multi
project_name: Wind-speed-regression
target_type: wind_ms #*
cmip_type: CMIP6 #*

model_name: GhostWindNet27  # {name}{time_window}x{half_side_size}
time_window: 27               # must match model, odd only
half_side_size: 47            # must match model, odd only

start_date: '2018-12-11'                   #for eval only
end_date: '2018-12-12'
