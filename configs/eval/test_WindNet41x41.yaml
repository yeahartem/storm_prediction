wind_risk_threshold: 20
use_elevation: false

lat_min: 30
lat_max: 70
lon_min: 30
lon_max: 90

interpolation_res: 0.25           #if None - does not apply interpolation

num_workers_eval: 32
batch_size_test: 20 
distributed_test: true
gpu_num: 8

save_kml: False                   #if false, saves to csv



data_dir: data/cmip5_world3/      # directory to get climate files for test and inference
target_data_file: target.parquet  # target data file in data_dir for training for test and inference

path_to_checkpoint: checkpoints/WindNet41x41_cmip5.ckpt
path_to_predictions: out/predictions/result_world.csv
output_file: out/predictions/output_file_world
cmip_type: CMIP5

