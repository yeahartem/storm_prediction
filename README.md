# Environments:
* `wind_env` - `environments/requirements.txt` -- most recent environment, better to use it.
# Data, NN
* Run `conf/train.py` to generate data and train NN model (see `conf/train_conf.json` for parameters)
* Run `conf/infer.py` to generate data to infer NN model on (see `conf/train_conf.json` for parameters, including path to pretrained NN)
